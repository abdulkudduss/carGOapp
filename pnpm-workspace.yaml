packages:
  - "packages/*"
  - "apps/*"

# pnpm 10+ does not run dependency build scripts unless they are allow-listed.
# These native packages need their install scripts to fetch/build binaries.
onlyBuiltDependencies:
  - esbuild
  - "@tailwindcss/oxide"
  - "@parcel/watcher"
