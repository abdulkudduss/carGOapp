# Expo's official recommendation for pnpm monorepos: use a hoisted node_modules
# layout so React Native's Metro bundler can resolve dependencies reliably.
# https://docs.expo.dev/guides/monorepos/#install-and-run-pnpm
node-linker=hoisted
strict-peer-dependencies=false
